# siyuan-query-builder

面向思源笔记的可视化查询构建器插件。

当前版本已包含：

- 可视化查询范围、条件、排序、分组、字段选择
- 查询模板保存与加载
- 表格、看板、列表、统计卡片视图
- 状态、日期、优先级的快速属性回写
- 文档块内渲染与嵌入描述块生成

开发：

```bash
npm install --legacy-peer-deps
npm run dev
```

测试：

```bash
npm run test:run
```
